#include<iostream>
using namespace std;
  
class node{
   int data;
   node* left=NULL;
   node* right=NULL;
   //constructor
   node(int d){
      this->data=d;
      this->left=left;
      this->right=right;
   }
};
node* buildTree(node*data){
      

   
   }
int main(){
   node* root=NULL;
   root=buildTree();
};